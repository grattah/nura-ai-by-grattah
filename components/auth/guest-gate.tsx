// Retired. RouteAuthGuard now renders the SignInModal directly (no opaque
// cover) so the page content shows behind it, blurred by the modal backdrop.
// Kept as an empty module during transition; safe to delete.
export {};
