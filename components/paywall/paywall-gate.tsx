// Retired. Content pages are no longer paywalled — recipe detail is public with
// component-level auth gating via <AuthGate>, and guides are protected pages
// handled by RouteAuthGuard. Kept as an empty module to avoid a dangling import
// during transition; safe to delete.
export {};
